# Server-side only: these variables never reach the browser (no NEXT_PUBLIC_ prefix).
# In AWS they come from Parameter Store, under /argon/<env>/.
API_URL=http://localhost:3001
INTERNAL_API_SECRET=change-me
