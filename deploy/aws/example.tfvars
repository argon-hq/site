# Copy to terraform.tfvars (ignored by git) and fill in. No secrets go here: parameter values
# live only in the Parameter Store.
alert_email   = "alertas@example.com"
budget_emails = ["alertas@example.com"]
