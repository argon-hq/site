{
  "name": "argon",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "scripts": {
    "certs": "node scripts/dev-certs.mjs",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.10.12"
  }
}
